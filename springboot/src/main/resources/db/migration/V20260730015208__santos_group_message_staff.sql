-- ============================================
-- Migration V20260730015208
-- ============================================
-- 설명: 관리자(스태프) 채팅 답장 구분 (plan 012) — group_message.staff 추가.
--       작성 시점에 확정: role은 변경될 수 있으므로 role 파생으로 판단하지 않는다
--       (관리자 강등 시 과거 메시지 라벨이 소급 변경되는 것을 방지).
--       기존 메시지는 모두 여행자 메시지이므로 DEFAULT FALSE로 백필 불필요.
-- 작성일: 2026-07-30 01:52:08
-- ============================================

ALTER TABLE group_message
    ADD COLUMN staff BOOLEAN NOT NULL DEFAULT FALSE;
